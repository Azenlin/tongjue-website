> 這份說明是給之後回來維護這個網站的阿任（或 Claude）看的，解釋為什麼程式碼分兩個地方放。

## 實際開發路徑

真正的 git repo、`npm install`、`npm run dev` 都在**本機**：

```
C:\Users\User\dev\tongjue-website
```

## 這個 Google Drive 資料夾是什麼

`G:\我的雲端硬碟\00_銅爵科技顧問\00_個人網站` 是原始碼的**唯讀備份鏡像**，不含 `node_modules/`、`dist/`、`.astro/`、`.git/`。

**為什麼分開放**：Google Drive 桌面版目前是「串流模式」，不是真正的本機 NTFS 磁碟，`npm install` 對 `node_modules` 大量小檔案寫入時會直接炸掉（`EPERM`/`ENOTEMPTY` 錯誤），symlink、junction 也建不起來繞過。實測過，這是硬限制不是偶發問題。所以改成本機開發、Drive 只留原始碼快照備份。

## 怎麼同步

在本機改完、commit 完之後，跑這行把最新原始碼鏡像回 Drive（PowerShell）：

```powershell
robocopy "C:\Users\User\dev\tongjue-website" "G:\我的雲端硬碟\00_銅爵科技顧問\00_個人網站" /MIR /XD node_modules dist .astro .git /XF package-lock.json
```

**不要直接在 Drive 資料夾裡跑 `npm install` 或 `npm run dev`**，會失敗。

## 之後如果想解除這個限制

去 Google Drive 桌面版設定，把 `00_銅爵科技顧問` 這個資料夾（或更上層）改成「鏡像」模式而不是「串流」模式，讓它變成真正的本機磁碟區，才能直接在 Drive 路徑下開發。這需要你自己在 Google Drive App 裡操作。
